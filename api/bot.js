export default async function handler(req, res) { try { if (req.method !== "POST") return res.status(200).json({ ok: true }); const body = typeof req.body === "string" ? JSON.parse(req.body) : req.body; const message = body.message; if (!message || !message.text) return res.status(200).json({ ok: true }); const chatId = message.chat.id; const msg = message.text.trim(); let answer = ""; if (["/start","salom","hi","hello"].includes(msg.toLowerCase())) { answer = "Salom! Men Netrica AI yordamchisiman. Netrica xizmatlari haqida savol bering!"; } else { const r = await fetch("https://api.groq.com/openai/v1/chat/completions", { method: "POST", headers: { "Content-Type": "application/json", "Authorization": "Bearer " + process.env.GROQ_KEY }, body: JSON.stringify({ model: "llama-3.1-8b-instant", messages: [{ role: "system", content: "Sen Netrica IT kompaniyasi AI yordamchisisisan. Uzbek tilida 1-2 gapda aniq javob ber. Netrica: Veb sayt 500$ 3-7 kun, Bot 300$ 3-5 kun, Ilova 3000$ 30-60 kun, CRM 1000$ 14-30 kun. Sayt: netrica.uz. Tel: +998938229313." }, { role: "user", content: msg }] }) }); const d = await r.json(); answer = d.choices[0].message.content; } await fetch("https://api.telegram.org/bot" + process.env.BOT_TOKEN + "/sendMessage", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ chat_id: chatId, text: answer || "Javob topilmadi" }) }); return res.status(200).json({ ok: true }); } catch(e) { return res.status(200).json({ ok: true, error: e.message }); } }
