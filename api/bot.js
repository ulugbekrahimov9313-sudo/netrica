export default async function handler(req, res) { try { if (req.method !== "POST") return res.status(200).json({ ok: true }); const body = typeof req.body === "string" ? JSON.parse(req.body) : req.body; const message = body.message; if (!message || !message.text) return res.status(200).json({ ok: true }); const chatId = message.chat.id; const msg = message.text.trim(); let answer = ""; if (["/start"].includes(msg.toLowerCase())) { answer = "Assalomu alaykum! Netrica IT kompaniyasining AI yordamchisiga xush kelibsiz.\n\nMen sizga xizmatlarimiz, narxlar va boshqa savollar bo`yicha yordam beraman.\n\nQanday yordam kerak?"; } else { const r = await fetch("https://api.groq.com/openai/v1/chat/completions", { method: "POST", headers: { "Content-Type": "application/json", "Authorization": "Bearer " + process.env.GROQ_KEY }, body: JSON.stringify({ model: "llama-3.1-8b-instant", max_tokens: 200, messages: [{ role: "system", content: "Sen Netrica IT kompaniyasining professional AI savdo assistentisan. Mijozlar bilan do`stona, xushmuomila va professional muloqot qil. Har qanday savolga uzbek tilida aniq va qisqa javob ber. Imlo xatolariga yo`l qo`yma. Netrica: 3+ yil tajriba, 500+ loyiha. Xizmatlar: Veb sayt 500$ (3-7 kun), Korporativ sayt 1500$ (7-14 kun), Telegram bot 300$ (3-5 kun), Mobil ilova 3000$ (30-60 kun), CRM 1000$ (14-30 kun). Jamoa: Sardorbek Tursunbekov (Boshliq), Farux Muxtorov (O`rinbosar), Islomjon Yuldashev (PM), Ulugbek, Bobur, Fayoz (Dasturchilar). Sayt: netrica.uz. Tel: +998938229313. Mavzudan tashqari savollarga ham do`stona javob ber." }, { role: "user", content: msg }] }) }); const d = await r.json(); answer = d.choices[0].message.content; } await fetch("https://api.telegram.org/bot" + process.env.BOT_TOKEN + "/sendMessage", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ chat_id: chatId, text: answer }) }); return res.status(200).json({ ok: true }); } catch(e) { await fetch("https://api.telegram.org/bot" + process.env.BOT_TOKEN + "/sendMessage", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ chat_id: body.message.chat.id, text: "Kechirasiz, texnik xatolik. Iltimos +998938229313 ga murojaat qiling." }) }); return res.status(200).json({ ok: true }); } }
