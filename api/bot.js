export default async function handler(req, res) { try { if (req.method !== "POST") return res.status(200).json({ ok: true }); const body = typeof req.body === "string" ? JSON.parse(req.body) : req.body; const message = body.message; if (!message || !message.text) return res.status(200).json({ ok: true }); const chatId = message.chat.id; const msg = message.text.trim(); let answer = ""; if (["/start","salom!","hi","hello"].includes(msg.toLowerCase())) { answer = "Salom! Men Netrica AI yordamchisiman.\n\nXizmatlar:\n- Veb sayt: 500$\n- Telegram bot: 300$\n- Mobil ilova: 3000$\n- CRM: 1000$\n\nSavol bering!"; } else { const r = await fetch("https://api.groq.com/openai/v1/chat/completions", { method: "POST", headers: { "Content-Type": "application/json", "Authorization": "Bearer " + process.env.GROQ_KEY }, body: JSON.stringify({ model: "llama-3.1-8b-instant", max_tokens: 150, messages: [{ role: "system", content: "Sen Netrica AI. Uzbek tilida 2-3 gapda aniq javob ber. Netrica ishonchli IT kompaniya, 500+ loyiha, 3 yil tajriba. Xizmatlar: Veb sayt 500$, Bot 300$, Ilova 3000$, CRM 1000$. Tel: +998938229313. Sayt: netrica.uz." }, { role: "user", content: msg }] }) }); const d = await r.json(); answer = d.choices[0].message.content; } await fetch("https://api.telegram.org/bot" + process.env.BOT_TOKEN + "/sendMessage", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ chat_id: chatId, text: answer }) }); return res.status(200).json({ ok: true }); } catch(e) { return res.status(200).json({ ok: true }); } }
