export default async function handler(req, res) { if (req.method !== "POST") return res.status(405).end(); const { message } = req.body; const r = await fetch("https://api.groq.com/openai/v1/chat/completions", { method: "POST", headers: { "Content-Type": "application/json", "Authorization": "Bearer " + process.env.GROQ_KEY }, body: JSON.stringify({ model: "llama3-8b-8192", messages: [{ role: "system", content: "Sen Netrica AI. Netrica IT kompaniyasi haqida javob ber. Sayt: netrica.uz. Tel: +998938229313." }, { role: "user", content: message }] }) }); const d = await r.json(); res.json({ answer: d.choices?.[0]?.message?.content || JSON.stringify(d) }); }
