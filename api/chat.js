export default async function handler(req, res) { try { const body = typeof req.body === 'string' ? JSON.parse(req.body) : req.body; const message = body.message || 'salom'; if (message.trim().toLowerCase() === 'salom') { return res.json({ answer: 'Salom! Qanday yordam kerak?' }); } const r = await fetch('https://api.groq.com/openai/v1/chat/completions', { method: 'POST', headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer ' + process.env.GROQ_KEY }, body: JSON.stringify({ model: 'llama-3.1-8b-instant', messages: [{ role: 'system', content: 'Sen Netrica AI. Uzbek tilida qisqa javob ber. Netrica: Veb sayt 500 dollar, Bot 300 dollar, Ilova 3000 dollar, CRM 1000 dollar. Tel: +998938229313. Sayt: netrica.uz.' }, { role: 'user', content: message }] }) }); const d = await r.json(); res.json({ answer: d.choices?.[0]?.message?.content || JSON.stringify(d) }); } catch(e) { res.json({ answer: 'Xato: ' + e.message }); } }
