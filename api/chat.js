export default async function handler(req, res) { try { const body = typeof req.body === "string" ? JSON.parse(req.body) : req.body; const message = body.message || "salom"; const r = await fetch("https://api.groq.com/openai/v1/chat/completions", { method: "POST", headers: { "Content-Type": "application/json", "Authorization": "Bearer " + process.env.GROQ_KEY }, body: JSON.stringify({ model: "llama-3.1-8b-instant", messages: [{ role: "system", content: "Sen Netrica IT kompaniyasi rasmiy AI yordamchisisisan. Uzbek tilida qisqa va aniq javob ber. Salom desa: Salom! Qanday yordam kerak? de. Netrica xizmatlari: Veb sayt 500$, Korporativ sayt 1500$, Telegram bot 300$, Mobil ilova 3000$, CRM 1000$. Jamoa 6 nafar. Sayt: netrica.uz. Tel: +998938229313. Faqat Netrica haqida javob ber." }, { role: "user", content: message }] }) }); const d = await r.json(); res.json({ answer: d.choices?.[0]?.message?.content || d.error?.message || JSON.stringify(d) }); } catch(e) { res.json({ answer: "Xato: " + e.message }); } }
