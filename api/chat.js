export default async function handler(req, res) { try { const body = typeof req.body === "string" ? JSON.parse(req.body) : req.body; const msg = (body.message || "salom").trim(); const gr = ["salom","salom!","hi","hello","hey","assalomu alaykum"]; if (gr.includes(msg.toLowerCase())) return res.json({ answer: "Salom! Qanday yordam kerak?" }); const r = await fetch("https://api.groq.com/openai/v1/chat/completions", { method: "POST", headers: { "Content-Type": "application/json", "Authorization": "Bearer " + process.env.GROQ_KEY }, body: JSON.stringify({ model: "llama-3.1-8b-instant", messages: [{ role: "system", content: "Sen Netrica AI. Uzbek tilida 2-3 qator qilib aniq javob ber. Netrica: Veb sayt 500$, Korporativ 1500$, Bot 300$, Ilova 3000$, CRM 1000$. Jamoa: Sardorbek boshliq, Farux orinbosar, Islomjon PM, Ulugbek/Bobur/Fayoz dasturchi. Sayt: netrica.uz. Tel: +998938229313." }, { role: "user", content: msg }] }) }); const d = await r.json(); res.json({ answer: d.choices[0].message.content }); } catch(e) { res.json({ answer: "Xato: " + e.message }); } }
